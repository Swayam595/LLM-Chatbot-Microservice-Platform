# Base image
FROM python:3.10-slim

# Set environment variables
ENV PYTHONDONTWRITEBYTECODE 1
ENV PYTHONUNBUFFERED 1

# Install Poetry
RUN pip install --upgrade pip && pip install poetry

# Set work directory
WORKDIR /app

# Copy pyproject and install deps
COPY ./services/api_gateway/pyproject.toml ./services/api_gateway/poetry.lock* /app/
COPY ./shared /shared/
RUN poetry config virtualenvs.create false \
    && poetry install --no-interaction --no-ansi --no-root

# Copy service code
COPY ./services/api_gateway/app /app/app
COPY ./services/api_gateway/config.py /app/

# Copy .env file
COPY ./services/api_gateway/.env /app/.env

# Expose port
EXPOSE 8000

# Run server
CMD ["poetry", "run", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
